# 콘텐츠 추가 워크플로우

이 문서는 이 프로젝트에 새로운 적(Enemy), 맵(Map), 플레이어 캐릭터(Character)를 추가할 때의 작업 순서를 정리한다. 이 프로젝트는 프리팹을 코드에 직접 등록하지 않고, `ScriptableObject`, Addressables, JSON 데이터, Inspector 연결로 콘텐츠를 조립한다.

## 공통 원칙

1. 먼저 사용할 모델, 머티리얼, 애니메이션, 아이콘, 사운드 등 원본 에셋을 프로젝트에 가져온다.
2. 프리팹을 만든 뒤 필요한 컴포넌트와 Serialized Field를 Inspector에서 연결한다.
3. 런타임에서 참조하는 프리팹과 ScriptableObject는 Addressable로 등록하고, 필요한 라벨을 설정한다.
4. 설정 데이터의 식별자(`mapId`, `characterName`, 적 이름/태그)는 중복 없이 일관되게 사용한다.
5. Unity Console 오류 없이 Loading -> Lobby -> InGame 전체 흐름을 플레이하여 검증한다.

## 새 Enemy 추가

### 1. 기획과 에셋 준비

- 적 이름, 일반/보스 여부, 이동 방식, 공격 방식, 체력, 공격력, 드롭 보상, 출현 맵을 결정한다.
- 모델, Animator Controller, 애니메이션 클립(최소 Idle, Move, Attack, Hurt, Dead), 머티리얼을 준비한다.
- 새 이동/공격 규칙이 기존 모듈(`FollowMove`, `RandomMove`, `PatternMove`, `MeleeAttack`, `Shoot` 등)으로 표현되는지 먼저 확인한다.
- 기존 모듈로 표현할 수 없을 때만 `IEnemyBehavior` 기반의 새 모듈과 `EnemyBehaviorFactory` 연결을 구현한다.

### 2. Enemy 프리팹 구성

- 적 루트 오브젝트에 `EnemyController`, `Health`, `Rigidbody`, 충돌용 `Collider`, `EnemyReferenceHub`를 둔다.
- `EnemyController`의 `enemyName`과 `enemyTags`를 설정한다. 데이터 기반 스탯을 사용할 경우 이름과 태그가 `enemyData`의 키와 일치해야 한다.
- `Animator`를 연결하고 Animator 파라미터/상태 이름이 `Idle`, `Move`, `Attack`, `Hurt`, `Dead` 규약과 맞는지 확인한다.
- `EnemyVisual`이 필요한 외형이면 렌더러를 연결한다. 재질은 Identity에서 런타임에 주입된다.
- 경험치와 골드 드롭 프리팹, 기본 드롭량을 `EnemyController`에 연결한다.
- 체력 바가 필요하면 자식으로 World Space Canvas와 Slider를 만들고 `EnemyHealthBar`의 `hpSlider`를 연결한다.
- NavMesh 기반 이동 적은 Collider/Rigidbody 크기가 실제 모델과 맞는지 확인한다.

### 3. EnemyIdentity 생성 및 연결

- `Assets/Scriptable Data/Enemy Data/<적 이름>_<Boss|Melee|Ranged>/` 폴더를 만들고, 그 안에 `EnemyIdentity`와 이동/공격 모듈 데이터 에셋을 함께 둔다. 폴더 접미사는 해당 적이 보스인지, 일반 근접(Melee)인지, 원거리(Ranged)인지에 따라 정한다(`EnemyTag`의 `Melee`/`Ranged`/`Boss` 비트와 일치시킨다).
- `Prefab`에 새 적 프리팹의 Addressable 참조를 넣는다.
- `Tag`, `ObjectMat`, `AccessoryMat`, `brain`을 설정한다. `brain`은 대부분 `Assets/Scriptable Data/Enemy Module Data/`에 있는 공용 `EnemyBrain` 에셋(`Melee Enemy`, `Ranged Enemy`, `Boss Enemy` 등)을 재사용한다.
- `moveModule`과 `attackModules`에 사용할 모듈 데이터 에셋을 연결한다.
- 일반 적과 보스 적은 사용하는 태그와 모듈 구성을 명확히 구분한다.

### 4. 데이터 및 맵 편성

- `Assets/Resources/Data/`의 enemy 데이터에 적 이름과 태그 조합에 맞는 스탯을 추가한다. 디버그 모드가 꺼진 적은 이 데이터가 없으면 정상 초기화할 수 없다.
- 출현시킬 `MapScriptable`의 `enemyIdentityList`에 일반 적을, `bossIdentityList`에 보스를 추가한다.
- 특정 위치에만 배치할 적이면 맵 프리팹의 `GameMap > Predefined Enemies` 목록에 Identity와 Spawn Point를 함께 등록한다.

### 5. Enemy 검증

- 해당 맵 전투에서 적이 스폰되고 `EnemyController` 초기화 오류가 없는지 확인한다.
- 이동, 공격, 피격, 사망, 드롭, 풀 반환 후 재스폰을 각각 확인한다.
- Console에서 `EnemyController not found`, 모듈 생성 오류, `GetEnemyData` 오류, Animator 상태 오류가 없는지 확인한다.

## 새 Map 추가

### 1. 맵 기획과 레벨 에셋 준비

- 맵 ID, 스테이지 순서, 일반 전투/보스 전투 여부, 출현 적 구성, 플레이어 시작점, 적 스폰 수를 정한다.
- 바닥, 벽/장애물, 장식, 조명, 충돌체를 배치한다.
- NavMesh를 사용하므로 이동 가능한 바닥과 장애물 구성을 확정한 뒤 NavMesh를 Bake한다.

### 2. Map 프리팹 구성

- 맵 루트에 `GameMap` 컴포넌트와 `NavMeshSurface`를 둔다.
- `GameMap.floor`와 `GameMap.surface`를 연결한다. `floor`는 랜덤 NavMesh 위치 계산의 기준이 된다.
- 플레이어 시작 위치용 빈 오브젝트를 만들고 `playerSpawnPoint`에 연결한다.
- 일반 적 Spawn Point를 필요한 수만큼 만들고 `enemySpawnPoints` 목록에 연결한다. 전투 중 적 수보다 Spawn Point가 적으면 가능한 수만 스폰된다.
- 보스 맵이면 `bossSpawnPoint`를 만든다.
- 고정 배치 적이 필요하면 `predefinedEnemies`에 적 Identity와 Spawn Point를 한 쌍으로 등록한다.
- 현재 적 이동 구역은 적 스폰 위치를 중심으로 자동 계산되므로, 일반적인 `RandomMove`/`PatternMove` 적을 위해 수동 Patrol Point를 만들 필요는 없다.

### 3. MapScriptable 및 JSON 등록

- `Assets/Scriptable Data/Map Data/`에 `MapScriptable` 에셋을 만든다.
- `mapId`를 map JSON의 동일 스테이지 `mapId`와 정확히 일치시킨다.
- 일반 전투에 사용할 맵 프리팹을 `mapList`에, 보스 전투용 맵 프리팹을 `bossMap`에 연결한다.
- 로비에서 사용할 미리보기 맵이 있으면 `lobbyDummyMap`을 연결한다.
- 일반 적 후보는 `enemyIdentityList`, 보스 후보는 `bossIdentityList`에 등록한다.
- MapScriptable 에셋을 Addressable로 표시하고 `map_config` 라벨을 추가한다.
- `Assets/Resources/Data/`의 map 데이터에 스테이지 정보와 동일한 `mapId`를 추가한다. `MapManager`는 현재 맵 인덱스로 JSON을 읽은 후 그 `mapId`로 MapScriptable을 찾는다.
- 맵 프리팹과 보스 맵 프리팹도 Addressable로 등록한다.

### 4. 로비 연결

- 새 스테이지를 로비 선택 화면에 노출해야 한다면 `LobbyMapManager`가 사용하는 맵 목록/로비 더미 맵을 함께 등록한다.
- 로비 카메라는 스테이지 수와 세로 간격을 기준으로 이동하므로, 새 맵의 로비 표시 위치와 잠금 해금 조건도 확인한다.

### 5. Map 검증

- Loading 과정에서 `[MapManager] 맵 설정 로드 성공` 로그에 새 설정이 포함되는지 확인한다.
- 스테이지 진입 시 맵이 활성화되고 Player Spawn Point에서 캐릭터가 시작하는지 확인한다.
- NavMesh 위에서 적이 이동하고, 일반/보스 Spawn Point 및 고정 배치 적이 의도한 위치에 생성되는지 확인한다.
- 맵 클리어 후 풀 반환, Retry, Lobby 복귀 후 다시 진입하는 흐름을 확인한다.

## 새 Character 추가

### 1. 기획과 에셋 준비

- 캐릭터 이름, 선택 UI 순서, 아이콘, 구매 가격, 기본 스탯, 레벨 성장, 고유 효과를 결정한다.
- 인게임 모델, Animator Controller, 애니메이션, 발사체 프리팹, 로비 미리보기 더미를 준비한다.
- 공격 방식이 기존 `PlayerController`와 `PlayerAttack` 흐름으로 가능한지 확인한다. 새 고유 공격/스킬이 필요하면 기존 Player Module 또는 Skill Module 패턴을 따른다.

### 2. 플레이어 및 발사체 프리팹 구성

- 인게임 캐릭터 프리팹에는 기존 플레이어 프리팹과 동일하게 `PlayerController` 및 필수 Player Module, Collider/Rigidbody, Animator, 렌더러를 구성한다.
- 이동, 자동 공격, 피격/사망, 경험치 획득에 필요한 참조가 누락되지 않았는지 기존 캐릭터 프리팹과 비교한다.
- 새 투사체 프리팹은 기존 `Projectile` 또는 `FlyingProjectile` 계열의 컴포넌트, Collider, Rigidbody, 시각 효과를 구성한다.
- 인게임 캐릭터 프리팹, 투사체 프리팹, 로비 더미 프리팹을 모두 Addressable로 등록한다.

### 3. CharacterIdentity 생성 및 연결

- `Assets/Scriptable Data/Character Identity/`에 `CharacterIdentity` 에셋을 만든다.
- `characterName`은 고유한 저장 키이며, 현재 캐릭터 설정 및 해금 데이터와 같은 문자열을 사용해야 한다.
- `index`를 캐릭터 선택 화면에 표시할 순서로 지정하고 `characterIcon`을 연결한다.
- `characterPrefab`, `projectilePrefab`, `lobbyCharacterDummy`에 각 Addressable 프리팹 참조를 연결한다.
- `baseStat`, `maxLevel`, `levelStatGrowth`, `levelUpCosts`를 설정한다. `maxLevel - 1`회 성장할 수 있도록 비용 배열 길이를 확인한다.
- `effectConfigs`와 `effectGrowths`에 캐릭터 고유 효과와 성장값을 입력한다.
- `purchasePrice`를 설정한다.
- CharacterIdentity 에셋을 Addressable로 표시하고 `character_identity` 라벨을 추가한다.

### 4. 해금 및 로비 UI 연결

- 새 캐릭터는 `CharacterManager`가 `character_identity` 라벨의 모든 Identity를 읽어 이름으로 캐시한다. 라벨 또는 이름이 누락되면 선택/스폰할 수 없다.
- 로비 캐릭터 탭에서 아이콘, 스탯, 구매 가격, 레벨업 비용, 로비 더미 모델이 정상 표시되는지 확인한다.
- 기본 지급 캐릭터가 아니라면 구매/해금 후 세이브 데이터의 `ownedCharacters`에 이름이 기록되는 흐름을 확인한다.
- 개발 중 즉시 선택하려면 현재 캐릭터 이름과 해금 목록이 새 `characterName`을 참조하도록 테스트 데이터를 조정한다. 배포용 기본 세이브를 임의로 변경하지 않는다.

### 5. Character 검증

- Loading 로그에 새 CharacterIdentity가 로드되는지 확인한다.
- 로비에서 구매, 선택, 레벨업, 아이콘/더미 표시를 확인한다.
- InGame에서 새 캐릭터가 Player Spawn Point에 생성되고, 기본 스탯 및 레벨 성장 스탯이 적용되는지 확인한다.
- 발사체 생성, 피격/사망, Retry, Lobby 복귀 후 재선택까지 확인한다.

## 최종 점검표

- 모든 새 프리팹과 참조 대상이 Addressable로 등록되어 있다.
- MapScriptable에는 `map_config`, CharacterIdentity에는 `character_identity` 라벨이 있다.
- `mapId`, `characterName`, 적 이름/태그가 중복되지 않고 JSON 및 ScriptableObject 참조와 일치한다.
- Inspector의 필수 참조에 `None`이 없다.
- 새 콘텐츠를 포함한 Loading -> Lobby -> InGame -> Clear/Death -> Retry/Lobby 흐름에서 Console Error가 없다.
- 에디터와 실제 빌드에서 Addressables 빌드 후 동일하게 로드된다.