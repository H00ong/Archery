namespace Players
{
    public enum PlayerState
    {
        Idle,
        Move,
        Attack,
        Dead
    }

    public enum SkillRarity
    {
        Common,
        Rare,
        Epic,
        Legend,
    }
}

